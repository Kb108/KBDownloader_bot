[DEPLOY (2).md](https://github.com/user-attachments/files/32220640/DEPLOY.2.md)
# ডিপ্লয় গাইড — কোনো কোডিং লাগবে না, শুধু কপি-পেস্ট ও ক্লিক

সব ফাইল রেডি করা আছে। আপনাকে শুধু ৩টা ওয়েবসাইটে গিয়ে কিছু ক্লিক ও কপি-পেস্ট করতে হবে। মোট সময় লাগবে ~১৫ মিনিট।

---

## 🔹 ধাপ ১: বট টোকেন নিন (২ মিনিট)
1. টেলিগ্রামে **@BotFather** খুলুন
2. `/newbot` পাঠান
3. একটা নাম দিন (যেমন: Target DEO Downloader)
4. একটা ইউজারনেম দিন, শেষে `bot` থাকতে হবে (যেমন: `targetdeo_video_bot`)
5. যে টোকেনটা পাবেন (যেমন `7123456789:AAExxxx...`) — এটা একটা নোটপ্যাডে কপি করে রাখুন

---

## 🔹 ধাপ ২: GitHub এ ফাইলগুলো তুলুন (৫ মিনিট)
1. [github.com/join](https://github.com/join) এ ফ্রি অ্যাকাউন্ট বানান (না থাকলে)
2. লগইন করে [github.com/new](https://github.com/new) এ যান
3. Repository name দিন: `video-downloader-bot`
4. **Public** সিলেক্ট করুন → **Create repository** ক্লিক করুন
5. এরপর পেজে **"uploading an existing file"** লিংকে ক্লিক করুন
6. আমি যে ৪টা ফাইল দিয়েছি — সবগুলো একসাথে ড্র্যাগ করে ছেড়ে দিন:
   - `bot.py`
   - `requirements.txt`
   - `nixpacks.toml`
   - `Procfile`
7. নিচে **"Commit changes"** বাটনে ক্লিক করুন

✅ এখন আপনার কোডটা GitHub এ আছে।

---

## 🔹 ধাপ ৩: Railway তে ডিপ্লয় করুন (৫ মিনিট)
1. [railway.app](https://railway.app) এ যান
2. **"Login with GitHub"** দিয়ে সাইন আপ করুন
3. ড্যাশবোর্ডে **"New Project"** ক্লিক করুন
4. **"Deploy from GitHub repo"** সিলেক্ট করুন
5. যদি প্রথমবার হয়, Railway কে আপনার GitHub Repository অ্যাক্সেস দিতে বলবে — **"Configure GitHub App"** ক্লিক করে `video-downloader-bot` রিপোটা সিলেক্ট করুন
6. এরপর লিস্ট থেকে **`video-downloader-bot`** ক্লিক করুন — ডিপ্লয় শুরু হয়ে যাবে

---

## 🔹 ধাপ ৩.৫: বটকে চ্যানেলের Admin বানান (জরুরি! — Force Subscribe এর জন্য)
বট যেন চেক করতে পারে কে জয়েন করেছে, তার জন্য বটকে আপনার **@loot_dells** চ্যানেলের Admin হতে হবে:
1. আপনার চ্যানেল **@loot_dells** এ যান
2. চ্যানেলের নামের উপর ক্লিক করুন → **"Administrators"** → **"Add Admin"**
3. আপনার বটের ইউজারনেম সার্চ করে সিলেক্ট করুন (যেমন `@targetdeo_video_bot`)
4. পারমিশন লিস্টে যেকোনো একটা টিক দিলেই হবে (যেমন "Post Messages") → **"Done"**

⚠️ এই ধাপ বাদ দিলে বট মেম্বারশিপ চেক করতে পারবে না এবং সবাইকে "জয়েন করেননি" দেখাবে, এমনকি জয়েন করা থাকলেও।

---

## 🔹 ধাপ ৪: টোকেন বসান (১ মিনিট)
1. ডিপ্লয় হওয়া প্রজেক্টের ভেতরে ঢুকুন
2. উপরে **"Variables"** ট্যাবে ক্লিক করুন
3. **"New Variable"** ক্লিক করুন
4. বসান:
   - Name: `BOT_TOKEN`
   - Value: (ধাপ ১ এ কপি করা টোকেনটা পেস্ট করুন)
5. **"Add"** ক্লিক করুন

### ব্রডকাস্ট (সবাইকে মেসেজ পাঠানো) ফিচার চালু করতে
আপনি চাইলে বট ব্যবহার করা সবাইকে একসাথে মেসেজ পাঠাতে পারবেন (নতুন ভিডিও, অফার ইত্যাদি জানাতে)। এর জন্য:
1. টেলিগ্রামে **@userinfobot** কে মেসেজ দিন — ও আপনাকে আপনার নিজের সংখ্যার আইডি দেবে (যেমন: `123456789`)
2. Railway এর **Variables** এ আরেকটা Variable যোগ করুন:
   - Name: `ADMIN_ID`
   - Value: (আপনার নিজের আইডি নাম্বারটা বসান)

### YouTube / প্রাইভেট Facebook-Instagram ডাউনলোড কাজ না করলে (খুব কমন সমস্যা)
YouTube ক্লাউড সার্ভার (Railway এর মতো) থেকে আসা রিকোয়েস্টকে "bot" ধরে নিয়ে ব্লক করে দেয় ("Sign in to confirm you're not a bot")। আর Facebook/Instagram এর কিছু প্রাইভেট বা ফলোয়ার-অনলি কন্টেন্ট লগইন ছাড়া দেখাই যায় না। দুটো সমস্যারই সমাধান একই — আপনার ব্রাউজারের লগইন করা cookies ব্যবহার করা:

1. Chrome এ **"Get cookies.txt LOCALLY"** এক্সটেনশন ইনস্টল করুন ([Chrome Web Store লিংক](https://chromewebstore.google.com/detail/get-cookiestxt-locally/cclelndahbckbenkjhflpdbgdldlbecc))
2. একই ব্রাউজারে গিয়ে **YouTube, Facebook এবং Instagram — প্রতিটাতে নিজের অ্যাকাউন্ট দিয়ে লগইন করুন** (তিনটাই একসাথে করলে একটাই cookies ফাইলে সব কাজ হয়ে যাবে)
3. যেকোনো একটা সাইটে থেকে এক্সটেনশনে ক্লিক করে **"Export"** করুন — এতে `cookies.txt` নামের একটা ফাইল ডাউনলোড হবে (এতে যে সাইটগুলোতে লগইন করা আছে তার সবকটার cookies একসাথে থাকবে)
4. ফাইলটা **Notepad** দিয়ে খুলুন, ভেতরের পুরো লেখা কপি করুন
5. Railway এর **Variables** এ নতুন একটা Variable যোগ করুন:
   - Name: `SITE_COOKIES`
   - Value: (কপি করা পুরো cookies.txt এর লেখাটা পেস্ট করুন)
6. **"Add"** ক্লিক করুন — বট রিডিপ্লয় হবে এবং এরপর থেকে YouTube ও প্রাইভেট Facebook/Instagram কন্টেন্ট ডাউনলোড কাজ করা উচিত (যেসব কন্টেন্টে আপনার লগইন করা অ্যাকাউন্টের অ্যাক্সেস আছে, সেগুলোই — সম্পূর্ণ আনরিলেটেড কারো প্রাইভেট অ্যাকাউন্ট এভাবে কাজ করবে না)

⚠️ **সতর্কতা**: এই cookies আপনার অ্যাকাউন্টগুলোর লগইন তথ্য বহন করে। এটা কখনো GitHub এ কমিট করবেন না — শুধু Railway এর Variables এ (যেটা গোপন থাকে) বসান। মাঝে মাঝে (কয়েক মাস পরপর) এটা আপডেট করার দরকার হতে পারে যদি আবার একই এরর আসে।

এটা সেভ হওয়ার সাথে সাথে Railway অটোমেটিক আবার ডিপ্লয় করবে (redeploy)।

---

## 🔹 ধাপ ৫: যাচাই করুন বট চালু হয়েছে কিনা
1. প্রজেক্টে **"Deployments"** ট্যাবে ক্লিক করুন
2. সর্বশেষ ডিপ্লয়মেন্টে ক্লিক করে **"View Logs"** দেখুন
3. লগে লেখা দেখবেন: `বট চালু হয়েছে...`

✅ **এখন বট সম্পূর্ণ চালু, ২৪/৭, আপনার কম্পিউটার বন্ধ থাকলেও চলবে।**

---

## 🔹 ব্যবহার করুন
টেলিগ্রামে গিয়ে আপনার বট (t.me/আপনার_বট_ইউজারনেম) ওপেন করুন → `/start` দিন → যেকোনো Facebook/YouTube/Instagram/TikTok লিংক পাঠান → ভিডিও চলে আসবে।

এই লিংকটা (t.me/আপনার_বট_ইউজারনেম) যে কাউকে শেয়ার করলেই তারাও ব্যবহার করতে পারবে।

---

## কিছু আটকে গেলে
- **Logs এ Error দেখাচ্ছে?** — লগের লেখাটা কপি করে আমাকে পাঠান, আমি দেখে বলে দেব সমাধান কী
- **ভিডিও পাঠাচ্ছে না?** — বেশিরভাগ সময় এটা হয় লিংকটা প্রাইভেট হলে, বা ৫০MB এর বেশি সাইজ হলে
- **খরচ**: Railway এর ফ্রি প্ল্যানে শুরুতে কিছু ব্যালেন্স ফ্রি দেওয়া হয়; বেশি ইউজার ব্যবহার করলে মাসে সামান্য খরচ (কয়েক ডলার) লাগতে পারে

---

## সবাইকে মেসেজ পাঠাবেন কীভাবে? (Broadcast)
`ADMIN_ID` সেট করা থাকলে, আপনি (শুধু আপনি) নিচের যেকোনো একভাবে সবাইকে মেসেজ পাঠাতে পারবেন:

**পদ্ধতি ১ — সরাসরি টেক্সট:**
বটকে মেসেজ করুন:
```
/broadcast আপনার মেসেজটা এখানে লিখুন
```

**পদ্ধতি ২ — ছবি/ভিডিও/ফরওয়ার্ড সহ পাঠাতে:**
1. যে মেসেজটা (ছবি, ভিডিও, বা টেক্সট) পাঠাতে চান সেটা বটে পাঠান বা বটের চ্যাটে রাখুন
2. সেই মেসেজের উপর **Reply** করে লিখুন: `/broadcast`

বট তখন এখন পর্যন্ত যত মানুষ `/start` দিয়েছে, সবাইকে সেই মেসেজ পাঠিয়ে দেবে এবং শেষে জানাবে কতজনকে পাঠানো হয়েছে।

⚠️ **নোট**: এই কমান্ড শুধু আপনার আইডি (`ADMIN_ID`) থেকে কাজ করবে — অন্য কেউ ব্যবহার করার চেষ্টা করলে বট চুপ থাকবে, কিছু হবে না।
